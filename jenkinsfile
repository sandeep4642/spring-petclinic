@Library('jenkins-shared-library')_

pipeline {
    agent any

stages {


  stage('Demo') {
  when { allOf { environment name: 'app_type', value: 'java'} }  
      steps {
          script {
               build.mavenbuild()

        }
    }
    
  }
}
} 
